namespace OpenNIX
{
    public static partial class Commands
    {
        public static void Help()
        {
            Console.WriteLine("help: All commands: ?, help, info, clear, reboot, shutdown, setfont, who, ls, cd, touch, mkdir, cat, rm, rmdir, onvi, gui, stopgui, setres, cp, log, crashsystem, license, script", SVGAIIColor.Gray);
        }
    }
}
